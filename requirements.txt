# Core DSP / math
# Note: 1.26.4 is the most stable version on Raspberry Pi OS (avoids libopenblas issues)
numpy>=2.1.3

# Audio
sounddevice==0.5.3

# GPIO + SPI + I2C
RPi.GPIO==0.7.1
spidev==3.8
gpiozero==2.0.1
colorzero==2.0
adafruit-circuitpython-mcp230xx

# OLED display (SPI)
luma.oled==3.13.0
luma.core==2.4.2
pillow>=11.0.0

# DMX / OLA
protobuf==3.20.3

# Utility deps
cffi==2.0.0
pycparser==2.23
pyserial==3.5

# SVG support for OLED splash screen
cairosvg==2.7.1
